<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/type_menuItem"
        android:title="@string/search"
        android:icon="@drawable/ic_list"
        app:showAsAction="ifRoom"
        app:iconTint="@color/black"
        tools:targetApi="o" />
    <item android:id="@+id/shoppingCart_menuItem"
        app:actionLayout="@layout/custom_action_bag_item_layout"
        android:title="@string/shopping_cart"
        android:icon="@drawable/ic_shopping_bag"
        app:showAsAction="ifRoom"
        app:iconTint="@color/black"
        tools:targetApi="o" />
    <item android:id="@+id/search_menuItem"
        android:title="@string/search"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom"
        app:iconTint="@color/black"
        tools:targetApi="o" />
</menu>